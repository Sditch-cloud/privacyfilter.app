{
  "name": "privacyfilter-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro preview",
    "deploy": "pnpm build && wrangler deploy",
    "desktop:assets": "node ./scripts/prepare-desktop-assets.mjs",
    "desktop:serve": "node ./src/desktop/server.mjs",
    "desktop:start": "pnpm build && pnpm desktop:assets && node ./src/desktop/server.mjs",
    "desktop:build:win": "pnpm build && pnpm desktop:assets && bun build ./src/desktop/server.mjs --compile --outfile ./release/privacyfilter.exe"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@huggingface/transformers": "^4.2.0",
    "astro": "^5.13.5",
    "tesseract.js": "^7.0.0",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "undici": "^7.16.0",
    "@tailwindcss/vite": "^4.2.4",
    "tailwindcss": "^4.2.4",
    "wrangler": "^4.0.0"
  }
}
